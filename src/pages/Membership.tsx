import PageShell from './PageShell'

export default function Membership() {
  return (
    <PageShell title="Membership">
      <p>
        Membership connects you to resources, events, and peer networks. Details
        on categories, fees, and how to apply will live here as your content is
        finalised.
      </p>
    </PageShell>
  )
}
